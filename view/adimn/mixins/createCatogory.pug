mixin tree(records,level=1)
    each item in records
        - const s= Array(level+1).join("-- ")
        option(value=item._id) #{s}#{item.title}
        if item.children && item.children.length>0
            +tree(item.children,level+1)